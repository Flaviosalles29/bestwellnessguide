# DISABLED - this workflow should be deleted.
#
# It emailed a weekly report built from counters that the daily automation
# inflated with pitches it never sent, and from directories (ready-to-send/,
# published/) that do not exist. With the daily workflow disabled there is
# nothing left for it to summarise.
#
# The schedule trigger is removed so this cannot fire. Delete the file with:
#   git rm .github/workflows/weekly-summary.yml

name: Weekly Summary (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow does nothing
        run: |
          echo "Disabled: this reported counters that were never backed by real activity."
          echo "See the comment at the top of the file. Delete it."
          exit 1
